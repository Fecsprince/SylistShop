@model IEnumerable<Microsoft.AspNet.Identity.EntityFramework.IdentityRole>
@{
    ViewBag.Title = "Add Role";
}
<div id="bucket">
    <div class="table-responsive">
        <table class="table table-bordered table-hover">
            <tr>
                <td align="center" colspan="2">
                    <h2> Create User Roles</h2>
                </td>

            </tr>

            <tr>
                <td>
                    <table>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td style="width:100%; border:dotted 1px;">
                                    @item.Name
                                </td>
                            </tr>
                        }
                    </table>
                </td>
                <td align="right" style="color:#FFFFFF;padding-right:10px;">


                    <h3>   @Html.ActionLink("Click to Create New Role", "Create", "Role", new { @class = "btn btn-primary" }) </h3>
                </td>
            </tr>
        </table>
    </div>
    @Html.ActionLink("User Role", "RoleAssignment", "Role", null, new { @class = "btn btn-primary" })
</div>